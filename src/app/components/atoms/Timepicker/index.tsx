import * as React from 'react';

export interface ITimePickerProps {
}

export default function TimePicker (props: ITimePickerProps) {
  return (
    <div>
      <p>Timepicker component</p>
    </div>
  );
}
